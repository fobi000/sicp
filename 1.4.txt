so a procedure name "a-plus-abs-b" is defined to take
two arguments(a and b), the arguments are
inputted into an expression which will adds/subtracts 
the arguments depending on the output of the 
if statement.